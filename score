Ок! Додаємо **рахунок (score)** у твою гру.
Я покажу **точно, що і куди додати**, нічого зайвого.

---

# ✅ 1. **У `snake.py` → у клас Game додаємо лічильник**

У `__init__` або `reset()`:

```python
self.score = 0
```

Я рекомендую саме **в reset()**, щоб рахунок скидався при смерті:

```python
def reset(self):
    mid_r = ROWS // 2
    mid_c = COLS // 2

    self.body = [
        {"pos": (mid_r, mid_c), "type": "straight"},
        {"pos": (mid_r, mid_c - 1), "type": "straight"},
        {"pos": (mid_r, mid_c - 2), "type": "straight"},
    ]
    self.dir = (0, 1)
    self.next_dir = (0, 1)
    self.place_apple()

    self.score = 0  # ← додано
```

---

# ✅ 2. **У update() додаємо +1 коли з’їдає яблуко**

Знайди рядок:

```python
if new_head == self.apple:
    self.place_apple()
```

Заміни на:

```python
if new_head == self.apple:
    self.score += 1      # ← додано
    self.place_apple()
```

---

# ✅ 3. **У main.py малюємо текст SCORE**

У `main.py`, прямо перед `game.draw(screen)`:

### (Створити шрифт один раз після pygame.init()):

Після:

```python
pygame.init()
clock = pygame.time.Clock()
```

додай:

```python
font = pygame.font.SysFont("arial", 32, True)
```

---

# 🔥 4. У головному циклі → після `game.draw(screen)` намалюємо рахунок

У main.py:

```python
game.draw(screen)

# намалювати рахунок
score_text = font.render(f"Score: {game.score}", True, (0, 0, 0))
screen.blit(score_text, (10, 10))

pygame.display.flip()
```

---

# 🎯 Що було змінено / додано?

| Де                    | Що додали         | Навіщо                       |
| --------------------- | ----------------- | ---------------------------- |
| `snake.py → reset()`  | `self.score = 0`  | Скидає рахунок при старті    |
| `snake.py → update()` | `self.score += 1` | +1 за з’їдене яблуко         |
| `main.py`             | Шрифт `font`      | Для відображення тексту      |
| `main.py`             | Малювання score   | Виводить на екран "Score: X" |

---

Хочеш — можу додати:
✔ рекорд (best score)
✔ таблицю результатів
✔ красивий UI з рамкою
✔ анімацію при наборі очків

Сказати “додай рекорд”?
https://chatgpt.com/share/69197d69-9b5c-8005-92d4-0de77add0cca
